siege_utilities.distributed.hdfs_legacy
=======================================

.. py:module:: siege_utilities.distributed.hdfs_legacy

.. autoapi-nested-parse::

   Fixed HDFS Operations - Minimal working version



Functions
---------

.. autoapisummary::

   siege_utilities.distributed.hdfs_legacy.check_hdfs_status
   siege_utilities.distributed.hdfs_legacy.get_quick_file_signature
   siege_utilities.distributed.hdfs_legacy.log_error
   siege_utilities.distributed.hdfs_legacy.log_info


Module Contents
---------------

.. py:function:: check_hdfs_status()

   Check if HDFS is accessible


.. py:function:: get_quick_file_signature(file_path)

   """
   Perform file operations: get quick file signature.

   Part of Siege Utilities File Operations module.
   Auto-discovered and available at package level.

   :returns: Description needed

   .. rubric:: Example

   >>> import siege_utilities
   >>> result = siege_utilities.get_quick_file_signature()
   >>> print(result)

   .. note::

      This function is auto-discovered and available without imports
      across all siege_utilities modules.

   """


.. py:function:: log_error(msg)

   """
   Log a message using the error level.

   Part of Siege Utilities Logging module.
   Auto-discovered and available at package level.

   :returns: Description needed

   .. rubric:: Example

   >>> import siege_utilities
   >>> result = siege_utilities.log_error()
   >>> print(result)

   .. note::

      This function is auto-discovered and available without imports
      across all siege_utilities modules.

   """


.. py:function:: log_info(msg)

   """
   Log a message using the info level.

   Part of Siege Utilities Logging module.
   Auto-discovered and available at package level.

   :returns: Description needed

   .. rubric:: Example

   >>> import siege_utilities
   >>> result = siege_utilities.log_info()
   >>> print(result)

   .. note::

      This function is auto-discovered and available without imports
      across all siege_utilities modules.

   """


